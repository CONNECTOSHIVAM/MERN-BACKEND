<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit File Name</title>
    <script src="https://cdn.jsdelivr.net/npm/@tailwindcss/browser@4"></script>
</head>
<body class="bg-black">

    <div class="main w-full min-h-screen bg-zinc-900 p-10 text-white">

        <a class="text-blue-500 inline-block mb-10" href="/">← Go back to home</a>

        <form action="/edit" method="POST">

            <!-- Show previous name but readonly -->
            <label class="block mb-1 text-sm">Previous File Name</label>
            <input 
                type="text"
                class="block px-5 w-1/4 rounded-md outline-none py-2 bg-zinc-800"
                value="<%= filename %>"
                readonly
            >

            <!-- hidden input actually used in POST -->
            <input type="hidden" name="previous" value="<%= filename %>">

            <label class="block mt-4 mb-1 text-sm">New File Name</label>
            <input 
                type="text"
                class="block px-5 w-1/4 rounded-md outline-none py-2 bg-zinc-800"
                placeholder="Enter new file name"
                name="new"
                required
            >

            <input 
                class="mt-4 bg-yellow-600 px-5 py-2 rounded-md cursor-pointer"
                type="submit" 
                value="Update Filename"
            >
        </form>
    </div>

</body>
</html>
